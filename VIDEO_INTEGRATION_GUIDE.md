# Руководство по интеграции видео на главной странице

## 📝 Описание

Видео для главной страницы (Hero) и Партнеров (Відеоогляд) теперь загружаются из CMS вместо жёстко закодированных путей. Это позволяет администраторам легко менять видео без необходимости менять код.

## 🔄 Как работает интеграция

### Поток данных:

```
CMS /api/translate/main-page (GET)
    ↓
    { sections: [
        { key: 'hero', videoUrl: 'https://cloudinary.com/...', ... },
        { key: 'partners', videoUrl: 'https://cloudinary.com/...', ... }
    ]}
    ↓
getSections() в lib/sections.ts
    ↓
src/app/[locale]/page.tsx передаёт videoUrl компонентам
    ↓
HeaderVideo & PartnersVideo используют видео из CMS
```

## 📦 Изменённые файлы

### 1. **src/lib/sections.ts**
   - Добавлено поле `videoUrl?: string | null` в интерфейс `Section`
   - Теперь содержит информацию о видео для каждой секции

### 2. **src/app/[locale]/page.tsx**
   - Обновлена типизация компонентов: `React.FC<{ videoUrl?: string | null }>`
   - Теперь передаёт `videoUrl` в `HeaderVideo` и `Partners` компоненты

### 3. **src/app/components/HeaderVideo/HeaderVideo.tsx**
   - Добавлен параметр `videoUrl` в компонент
   - Если `videoUrl` предоставлен, использует его; иначе использует default видео
   ```tsx
   {videoUrl ? (
     <source src={videoUrl} type="video/mp4" />
   ) : (
     <>
       <source src="/bg-video-header.webm" type="video/webm" />
       <source src="/bg-video-header.mp4" type="video/mp4" />
     </>
   )}
   ```

### 4. **src/app/components/Partners/Partners.tsx**
   - Добавлен параметр `videoUrl` в компонент
   - Передаёт `videoUrl` в компонент `PartnersVideo`

### 5. **src/app/components/PartnersVideo/PartnersVideo.tsx**
   - Добавлен параметр `videoUrl` в компонент
   - Обновлена логика источников видео:
   ```tsx
   sources: videoUrl 
     ? [{ src: videoUrl, type: 'video/mp4' }]
     : (locale === 'en' ? VIDEO_SOURCES.en : VIDEO_SOURCES.uk)
   ```

## 🎯 Использование в CMS

### Загрузка видео:
1. Перейти на `/translate/main` в CMS
2. В панели "Відео" выбрать тип:
   - **Hero** - видео для главной секции (без звука, muted)
   - **Partners** - видео для Відеоогляду (со звуком, controls)
3. Загрузить видео файл (mp4, webm и т.д.)
4. Видео автоматически загружается на Cloudinary и сохраняется в БД

### Поддерживаемые форматы:
- mp4, webm, mov, avi, mkv, flv, wmv, m4v
- Максимальный размер: 100MB

## 🔄 Fallback механизм

Если в CMS не установлено видео или происходит ошибка:

- **Hero**: Использует default видео из `/bg-video-header.webm` и `/bg-video-header.mp4`
- **Partners**: Использует default видео в зависимости от локали (en/uk)

## 🚀 Технические детали

### Кэширование
- Секции кэшируются с тегом `tags: ['sections']`
- После обновления видео в CMS используется revalidate endpoint для обновления тега

### Типизация
Все компоненты имеют правильную типизацию:
```typescript
React.FC<{ videoUrl?: string | null }>
```

### Поддержка локалей
- Видео фетчится один раз для всех локалей
- Структура данных инвариантна относительно локали

## 📋 Проверка

Проект успешно компилируется:
```
✓ Compiled successfully
```

Все типы корректны, нет ошибок TypeScript.

## 🔗 Связанные документы

- [VIDEO_UPLOAD_GUIDE.md](./VIDEO_UPLOAD_GUIDE.md) - Руководство по загрузке видео в CMS
- [IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md) - Полное описание функционала видео
